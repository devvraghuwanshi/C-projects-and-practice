/*You are given an integer N. Write a C program using a switch-case statement that performs the following operations:

If N is divisible by both 3 and 5, print "FizzBuzz".
If N is only divisible by 3, print "Fizz".
If N is only divisible by 5, print "Buzz".
If N is not divisible by either 3 or 5, print "No FizzBuzz".
YOU CANNOT USE IF-ELSE*/

#include<stdio.h>
int main(){

 int N;

 printf("enter N :\n");
scanf("%d",&N



 }

    return 0;
}